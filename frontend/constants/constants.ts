export enum ADMIN_PAGE_TAB {
  USER_CONTROL = 0,
  KEYWORD_CONTROL = 1,
}

export enum KEYWORD_ADDER_THEME {
  MAIN = 'main',
  MODAL = 'modal',
}

export enum REACT_QUERY_KEY {
  KEYWORD = 'keyword',
  USERME = 'userme',
}
