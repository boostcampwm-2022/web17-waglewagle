import HomeLayout from '@components/home/HomeLayout';
import HomeTitle from '@components/home/HomeTitle';

const Home = () => {
  return (
    <HomeLayout>
      <HomeTitle />
    </HomeLayout>
  );
};

export default Home;
