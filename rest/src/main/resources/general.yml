spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: validate #create
    properties:
      hibernate:
        show_sql: true